# Spring_Property_File
Spring Boot property files (`application.properties` / `application.yml`) are used to manage application configuration such as database, server, security, JWT, and OAuth2 settings.
